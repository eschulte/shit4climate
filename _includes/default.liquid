<!DOCTYPE html>
<html class="s4c-fill">
  <head>
    <meta name="description" content="Script to discuss climate change with your representative.  Specifically, {{ title }}.">
    {% include head %}
    <style>
      {% capture css %}
      {% include w3 %}
      {% include s4c %}
      {% include bottom-cylinder %}
      {% endcapture %}
      {{ css | cssmin | safe }}
    </style>
  </head>
  <body class="s4c-fill" onload="outside_on_load()">
    <header>
      {% include top %}
      <button id="home" class="s4c-top-button"><a href="{{ "/"|url }}">Home</a></button>
      <button id="about" class="s4c-top-button" style="top:3.5em"><a href="{{ "/about"|url }}">about</a></button>
    </header>
    <div class="w3-container tp">
      <h1 id="title">{{ title }}</h1>
      <div id="contact"></div>
      <div id="content">
      {{ content | safe }}
      </div>
      <div class="cylinder"></div>
    </div>
  </body>
  <script src="{{ "/javascript.js"|url }}"></script>
</html>
