<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <meta charset="UTF-8">
    <meta name="description" content="Call you representatives to ensure they understand the importance of climate change and take seriously their unique position to address it.  It is imperative that the politicians at all levels recognize the historical, geological, existential importance of this moment and that they take immediate action commensurate to with the need.  It's time to transform society, now.  So whenever you take a shit with your phone, use this page to call them, and let them hear you flush.">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="{{ "/favicon.ico" |url }}">
    <style>
      {% capture css %}
      {% include w3 %}
      {% include s4c %}
      {% include cloud %}
      {% include tree %}
      {% include fire %}
      {% include construction %}
      {% include cylinder %}
      {% endcapture %}
      {{ css | cssmin | safe }}
    </style>
  </head>
  <body onload="populate_zip()">
    <header>
      {% include top %}
      <button id="home" class="s4c-top-button"><a href="{{ "/about"|url }}">about</a></button>
    </header>
    <div class="w3-container" style="max-width:800px;margin:auto;padding:1em;">
      {{ content | safe }}
    </div>
    {% include bottom %}
  </body>
  <script src="{{ "/javascript.js"|url }}"></script>
</html>
