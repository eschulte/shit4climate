<!DOCTYPE html>
<html class="s4c-fill">
  <head>
    <title>{{ title }}</title>
    <meta charset="UTF-8">
    <meta name="description" content="Script to discuss climate change with your representative.">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      {% capture css %}
      {% include w3 %}
      {% include s4c %}
      {% include cloud %}
      {% include construction %}
      {% endcapture %}
      {{ css | cssmin | safe }}
    </style>
  </head>
  <body class="s4c-fill" style="background-image: linear-gradient(skyblue, skyblue, gray);">
    <header>
      {% include top %}
      <div class="w3-bar w3-card s4c-blue">
        <a href="{{ "/" | url }}" class="w3-bar-item w3-button w3-wide">Home</a>
      </div>
    </header>
    <div class="w3-container" style="max-width:800px;margin:auto;">
      <h1 id="title">{{ title }}</h1>
      <div id="contact"></div>
      <blockquote>
        <b>NOTE:</b> First identify yourself as a
        <i>constituent</i> and give your <b>name</b>, <b>address</b>,
        and <b>phone number</b>.
      </blockquote>
      <div id="content">
      {{ content | safe }}
      </div>
    </div>
  </body>
  <script src="{{ "/zip-data.js"|url }}"></script>
  <script>
    {% capture js %}
    {% include outside-javascript %}
    {% endcapture %}
    {{ js | jsmin | safe }}
    write_emails()
  </script>
</html>
