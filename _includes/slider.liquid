<!-- -*- mode:mhtml -*- -->
<div class="slider">
<div class="cylinder"></div>
<div class="slides">
{% assign index = 0 %}
{% assign sorted = roll | sortByOrder %}
{% for piece in sorted %}
<a href="{{ piece.url| url }}">
<div id="slide-{{ index }}" class="w3-container">
<h3>{{ piece.data.title }}</h3>
<p class="w3-margin">{{ piece.data.brief|safe }}</p>
</div>
</a>
{% assign index = index| plus: 1 %}
{% endfor %}
</div>
</div>
